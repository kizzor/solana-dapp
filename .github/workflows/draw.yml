name: HEIST Draw Number
on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:
jobs:
  draw:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger draw endpoint
        run: |
          curl -s -X GET \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            https://ransomematrix.xyz/api/draw
